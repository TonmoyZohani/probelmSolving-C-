#include<iostream>
#include<bits/stdc++.h>
#include<cstring>
using namespace std;
int main(){
    /*char str[]="10234";
    int len,i,sum=0;
    len=strlen(str);
    cout<<len;

    int number=atoi(str);

    for(i=0;i<len;i++){
        sum+=str[i]-48;
    }

    cout<<sum; */

    int num=3,power;
    power=pow(num,3);

    cout<<power;

}
